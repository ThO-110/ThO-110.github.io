import { createMuiTheme } from "@material-ui/core";

const myTheme = createMuiTheme({
  palette: {
    type: "dark",
    primary: {
      main: "#1a1f24",
    },
    secondary: {
      main: "#6b5487",
    },
  },
});

export default myTheme;
